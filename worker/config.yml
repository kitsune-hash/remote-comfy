server:
  url: "${RUNQY_URL}"
  api_key: "${RUNQY_API_KEY}"

worker:
  queue: "comfy"
  concurrency: 1
  handler:
    command: "python3 /workspace/handler.py"
    mode: "long_running"
  timeouts:
    job: "20m"
    graceful_shutdown: "30s"
  heartbeat_interval: "30s"
